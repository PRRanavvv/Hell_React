import { useRouteError } from "react-router-dom";

const Error = () => {
  return (
    <div>
        <h1>Oops !!</h1>
        <h1>something went wrong</h1>
    </div>
  );
};

export default Error;